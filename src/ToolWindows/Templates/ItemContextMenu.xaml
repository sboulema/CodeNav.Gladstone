<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml"
                    xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
                    xmlns:colors="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0">

    <ContextMenu
        x:Key="ItemContextMenu"
        Style="{DynamicResource {x:Static styles:VsResourceKeys.ContextMenuStyleKey}}">

        <MenuItem Header="Go to Definition" Command="{Binding Path=GoToDefinitionCommand}">
            <MenuItem.Icon>
                <vs:Image Source="KnownMonikers.GoToDefinition" />
            </MenuItem.Icon>
        </MenuItem>

        <MenuItem Header="Go to End" Command="{Binding Path=GoToEndCommand}">
            <MenuItem.Icon>
                <vs:Image Source="KnownMonikers.HTMLEndTag" />
            </MenuItem.Icon>
        </MenuItem>

        <Separator />

        <MenuItem Header="Select In Code" Command="{Binding Path=SelectInCodeCommand}">
            <MenuItem.Icon>
                <vs:Image Source="KnownMonikers.BlockSelection" />
            </MenuItem.Icon>
        </MenuItem>

        <Separator />

        <MenuItem Header="Copy Name" Command="{Binding Path=CopyNameCommand}">
            <MenuItem.Icon>
                <vs:Image Source="KnownMonikers.Copy" />
            </MenuItem.Icon>
        </MenuItem>

        <Separator />

        <MenuItem Header="Refresh" Command="{Binding Path=RefreshCommand}">
            <MenuItem.Icon>
                <vs:Image Source="KnownMonikers.Refresh" />
            </MenuItem.Icon>
        </MenuItem>

        <Separator />

        <MenuItem Header="Expand All" Command="{Binding Path=ExpandAllCommand}">
            <MenuItem.Icon>
                <vs:Image Source="KnownMonikers.ExpandAll" />
            </MenuItem.Icon>
        </MenuItem>

        <MenuItem Header="Collapse All" Command="{Binding Path=CollapseAllCommand}">
            <MenuItem.Icon>
                <vs:Image Source="KnownMonikers.CollapseAll" />
            </MenuItem.Icon>
        </MenuItem>

        <Separator />

        <MenuItem Header="Clear History" Command="{Binding Path=ClearHistoryCommand}">
            <MenuItem.Icon>
                <vs:Image Source="KnownMonikers.CleanData" />
            </MenuItem.Icon>
        </MenuItem>

    </ContextMenu>

</ResourceDictionary>
